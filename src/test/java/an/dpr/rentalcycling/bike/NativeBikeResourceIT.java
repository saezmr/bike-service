package an.dpr.rentalcycling.bike;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeBikeResourceIT extends BikeResourceTest {

    // Execute the same tests but in native mode.
}